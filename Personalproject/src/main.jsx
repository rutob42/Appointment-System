// src/index.js
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';  // Import your App component

ReactDOM.render(
  <App />,  // App component already has the Router
  document.getElementById('root')
);
